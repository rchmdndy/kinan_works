import { chmodSync, mkdirSync, mkdtempSync, readFileSync, renameSync, rmSync, writeFileSync } from 'node:fs';
import { dirname, join } from 'node:path'; import { tmpdir } from 'node:os'; import type { Config } from './config.js'; import { decryptSecret } from './crypto.js'; import type { Repository } from './repository.js';
export type BrokerCredentials = { sync(): Promise<void> }; function safeId(value: string): boolean { return /^[a-zA-Z0-9_-]{8,64}$/.test(value); }
async function sha512Password(binary: string, username: string, secret: string): Promise<string> { const directory = mkdtempSync(join(tmpdir(), 'kinan-mqtt-')); const file = join(directory, 'passwd'); try { const child = Bun.spawn([binary, '-b', '-c', file, username, secret], { stdout: 'ignore', stderr: 'pipe' }); const stderr = await new Response(child.stderr).text(); if (await child.exited !== 0) throw new Error(`mosquitto_passwd failed: ${stderr.trim()}`); const line = readFileSync(file, 'utf8').trim(); if (!line.startsWith(`${username}:$7$`)) throw new Error('mosquitto_passwd returned an unsupported password hash'); return line; } finally { rmSync(directory, { recursive: true, force: true }); } }
function atomicWrite(path: string, content: string): void { mkdirSync(dirname(path), { recursive: true }); const temporary = `${path}.${process.pid}.${crypto.randomUUID()}.tmp`; writeFileSync(temporary, content, { mode: 0o644 }); chmodSync(temporary, 0o644); renameSync(temporary, path); }
export class MosquittoFileCredentials implements BrokerCredentials { constructor(private readonly repository: Repository, private readonly config: Config) {} async sync(): Promise<void> { const devices = this.repository.listAllDevices(); const passwordLines = [await sha512Password(this.config.MOSQUITTO_PASSWD_BIN, this.config.MQTT_INGEST_USERNAME, this.config.MQTT_INGEST_PASSWORD)]; const aclLines = [`user ${this.config.MQTT_INGEST_USERNAME}`, 'topic read devices/+/+/telemetry', '']; for (const device of devices) { if (!device.active) continue; if (!safeId(device.id)) throw new Error(`Invalid device ID in broker configuration: ${device.id}`); const encrypted = this.repository.getEncryptedSecret(device.id); if (!encrypted) throw new Error(`Encrypted secret unavailable for ${device.id}`); passwordLines.push(await sha512Password(this.config.MOSQUITTO_PASSWD_BIN, device.id, await decryptSecret(encrypted, this.config.encryptionKey))); aclLines.push(`user ${device.id}`, `topic write devices/${device.id}/${device.credentialVersion}/telemetry`, ''); } atomicWrite(this.config.MOSQUITTO_PASSWORD_FILE, `${passwordLines.join('\n')}\n`); atomicWrite(this.config.MOSQUITTO_ACL_FILE, `${aclLines.join('\n')}\n`); } }
export class NoopBrokerCredentials implements BrokerCredentials { async sync(): Promise<void> {} }
